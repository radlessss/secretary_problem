repos:
-   repo: local
    hooks:
    -   id: run-tests
        name: Run Unit Tests
        entry: python -m unittest discover -s src/tests -t src
        language: system
        pass_filenames: false